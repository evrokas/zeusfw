# zeusfw
Zeus CMS Framework for website development 

The basic idea of Zeus CMS is that all site information is stored on the filesystem.
They are loaded from the filesystem to the database, but their primary place is the filesystem.
This makes it easier for migration and update.
