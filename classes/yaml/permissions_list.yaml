- table: permissions_list
  class: permissionsListClass
  fields:
    - alpha: char(36) not null
    - beta: char(36) not null


# this is the list that connects guid
# used to connect alpha guid from users, roles, permissions entries
# to beta guid from users, roles, permissions entries
