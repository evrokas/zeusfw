table:
  name: roles
  class: rolesClass
  fields:
    - name: "guid"
      type: char(36)
      db_required: true
    - name: "text"
      type: char(32)
      db_required: true

# guid is the GUID of the role
# text is the name of the role
