{#
    parameters:
        region_name         name of the region
        blocks              string of rendered blocks output
    
#}

<div class="region region-{{$region_name}}">
    {{ $blocks }}
</div>
