<div class="user-block">
    {% if($loggedin): %}
        User <span><a href="{{rel_url('/profile')}}">{{$name}}</a></span>. <a href="{{rel_url('/logout')}}">Logout</a>
    {% else: %}
        <a href="{{rel_url('/login')}}">Login</a>
    {% endif %}
    
</div>